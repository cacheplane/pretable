node_modules
dist
.next
.turbo
coverage
playwright-report
test-results
status/runsets
status/snapshots
status/traces
pnpm-lock.yaml
*.tsbuildinfo

# api-extractor generates these — prettier would reformat and break api:check
*.api.md

# Prettier is non-idempotent on this file: it oscillates the indentation of
# fenced code nested inside `- [ ]` steps, so --write can never satisfy
# --check. Verified by three consecutive --write passes producing three
# different files. Planning docs are prose; excluding one is cheaper than
# restructuring 4k lines around a formatter bug.
docs/superpowers/plans/2026-08-09-dawn-browse-query-contract.md
