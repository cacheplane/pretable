import { defineExample } from "../../../lib/docs/examples/define";

export default defineExample({
  title: "Badge tones",
  description:
    "Four toned badges and a plain badge show how labels retain meaning with or without a tone.",
  files: ["BadgeTonesExample.tsx"],
  height: 240,
});
