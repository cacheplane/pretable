import { defineExample } from "../../../lib/docs/examples/define";

export default defineExample({
  title: "Search teammates",
  description:
    "A labelled native search input filters a small list as you type. Clear it to show everyone again.",
  files: ["SearchPeople.tsx"],
  height: 220,
});
