import { defineExample } from "../../../lib/docs/examples/define";

export default defineExample({
  title: "Status tones",
  description:
    "All five status tones pair a dot with a meaningful state label, including the neutral state.",
  files: ["StatusTonesExample.tsx"],
  height: 240,
});
