import { defineExample } from "../../../lib/docs/examples/define";

export default defineExample({
  title: "Mark a task done",
  description:
    "Toggle completion directly in the cell and inspect the saved boolean.",
  files: ["BooleanEditingGrid.tsx"],
  height: 340,
});
