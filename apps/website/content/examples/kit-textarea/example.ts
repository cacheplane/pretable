import { defineExample } from "../../../lib/docs/examples/define";

export default defineExample({
  title: "Write a handoff note",
  description:
    "A controlled textarea uses native rows and maxLength props. Save the note to preview its line breaks.",
  files: ["EditNote.tsx"],
  height: 320,
});
