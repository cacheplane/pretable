import { defineExample } from "../../../lib/docs/examples/define";

export default defineExample({
  title: "Rename a task",
  description:
    "Edit a task title with the built-in single-line text field and inspect the saved string.",
  files: ["TextEditingGrid.tsx"],
  height: 340,
});
