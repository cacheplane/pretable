import { defineExample } from "../../../lib/docs/examples/define";

export default defineExample({
  title: "Entity lines",
  description:
    "An identity with a secondary line sits beside one with only a primary line.",
  files: ["EntityLinesExample.tsx"],
  height: 240,
});
