# Pretable Docs Site Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the minimal `/docs` experience with a Mintlify-aligned docs site (3-col layout, MDX components, Cmd+K search, AI-forward features) while keeping Pretable's Alpenglow visual identity and supporting live grid demos.

**Architecture:** Custom Next.js 16 RSC pipeline using `next-mdx-remote/rsc`. MDX source moves to `apps/website/content/docs/`. Catch-all `/docs/[[...slug]]/page.tsx` renders any page; middleware rewrites `/docs/<slug>.md` to a plain-text branch of the same handler. Live demos via `<Example>` reading typed `defineExample()` modules from `apps/website/content/examples/`.

**Tech Stack:** Next.js 16 (App Router, Turbopack, RSC), React 19, TypeScript, Tailwind v4, `next-mdx-remote/rsc` 5.x, `rehype-slug`, `rehype-pretty-code` (Shiki, `github-light` theme), `remark-gfm`, `fuzzysort` 3.x, Vitest + React Testing Library, Playwright. Existing Alpenglow design tokens in `apps/website/app/globals.css`.

**Spec:** `docs/superpowers/specs/2026-05-05-docs-site-design.md`

---

## File Structure

**New library code (`apps/website/lib/docs/`)**

- `load.ts` — read + compile a single MDX file by slug array
- `enumerate.ts` — walk `content/docs/**/*.mdx`, return ordered slug list
- `extract-headings.ts` — parse raw MDX → `[{ depth, text, slug }]`
- `prev-next.ts` — given current slug, return prev/next neighbors via `_nav.ts`
- `define-example.ts` — `defineExample()` helper + `ExampleDef` type
- `search-index.ts` — build-time enumerate + body extract → JSON manifest
- `paths.ts` — slug↔file path mapping helpers, frontmatter validator

**New components (`apps/website/app/components/docs/`)**

- `DocsShell.tsx` — 3-col grid wrapper
- `DocsSidebar.tsx` — left nav, sticky, scrollable
- `DocsTOC.tsx` — right "On this page" + scrollspy
- `DocsSearch.tsx` — Cmd+K palette
- `DocsPageHeader.tsx` — H1 + description + CopyPageButton
- `DocsBreadcrumb.tsx` — group › title
- `DocsPrevNext.tsx` — prev/next links
- `DocsMobileDrawer.tsx` — sidebar drawer for <768px
- `MdxRenderer.tsx` — passes the MDX components map to `compileMDX`
- `CopyPageButton.tsx` — "Copy as Markdown"
- `CopyPromptButton.tsx` — inline on `<Prompt>`
- `mdx/Callout.tsx`, `mdx/Steps.tsx` (+`Step`), `mdx/Tabs.tsx` (+`Tab`), `mdx/CodeGroup.tsx`, `mdx/CodeBlock.tsx`, `mdx/Card.tsx` (+`CardGroup`), `mdx/Prompt.tsx`, `mdx/Frame.tsx`, `mdx/Example.tsx`

**New route handlers**

- `apps/website/app/docs/[[...slug]]/page.tsx` — catch-all docs page (replaces existing per-folder `page.mdx` files)
- `apps/website/app/docs/layout.tsx` — replaces existing
- `apps/website/middleware.ts` — `.md` extension rewrite
- `apps/website/app/llms.txt/route.ts`
- `apps/website/app/llms-full.txt/route.ts`
- `apps/website/app/docs/search-index.json/route.ts`

**Content migration**

- `apps/website/content/docs/**/*.mdx` — 24 files moved from `apps/website/app/docs/<group>/<slug>/page.mdx`
- `apps/website/content/examples/streaming-chat-grid/` — first live example folder

**Existing files modified**

- `apps/website/app/docs/_nav.ts` — keep as-is (source of truth)
- `apps/website/app/components/CodeExample.tsx` — re-targets to `<Example>` (Phase 13)
- `apps/website/app/components/NavBar.tsx` — accept optional `mobileDrawerSlot`
- `apps/website/next.config.ts` — add `Link` header for `/docs/*`, enable `?raw` imports if needed
- `apps/website/package.json` — add deps

**Files deleted (after migration)**

- All `apps/website/app/docs/<group>/[**]/page.mdx` files
- `apps/website/app/docs/<group>/[**]/layout.tsx` files (none exist outside root, but verify)

---

## Phase 0: Setup

### Task 0: Install dependencies

**Files:**

- Modify: `apps/website/package.json`

- [ ] **Step 1: Add deps**

```bash
pnpm --filter @pretable/app-website add next-mdx-remote rehype-slug rehype-pretty-code remark-gfm shiki fuzzysort gray-matter
pnpm --filter @pretable/app-website add -D @types/mdx
```

- [ ] **Step 2: Verify install**

Run: `pnpm --filter @pretable/app-website list next-mdx-remote rehype-slug rehype-pretty-code remark-gfm shiki fuzzysort`
Expected: each prints a version. No errors.

- [ ] **Step 3: Commit**

```bash
git add apps/website/package.json pnpm-lock.yaml
git commit -m "chore(website): add docs MDX pipeline deps"
```

---

## Phase 1: Foundation libraries (TDD)

### Task 1: `paths.ts` — slug↔file mapping + frontmatter type

**Files:**

- Create: `apps/website/lib/docs/paths.ts`
- Test: `apps/website/lib/docs/__tests__/paths.test.ts`

- [ ] **Step 1: Write failing tests**

```ts
// apps/website/lib/docs/__tests__/paths.test.ts
import { describe, it, expect } from "vitest";
import {
  slugToContentPath,
  contentPathToSlug,
  isValidSlugSegment,
} from "../paths";

describe("slugToContentPath", () => {
  it("maps empty slug to index.mdx", () => {
    expect(slugToContentPath([])).toBe("getting-started/index.mdx");
  });
  it("maps single segment to <segment>/index.mdx if dir, else <segment>.mdx", () => {
    // Dir-style preferred for groups; resolver handles both
    expect(slugToContentPath(["grid"])).toBe("grid/index.mdx");
  });
  it("maps nested slug to nested file", () => {
    expect(slugToContentPath(["grid", "pretable-component"])).toBe(
      "grid/pretable-component.mdx",
    );
  });
});

describe("contentPathToSlug", () => {
  it("strips .mdx and index", () => {
    expect(contentPathToSlug("grid/index.mdx")).toEqual(["grid"]);
    expect(contentPathToSlug("grid/pretable-component.mdx")).toEqual([
      "grid",
      "pretable-component",
    ]);
    expect(contentPathToSlug("getting-started/index.mdx")).toEqual([]);
  });
});

describe("isValidSlugSegment", () => {
  it("accepts kebab-case", () => {
    expect(isValidSlugSegment("pretable-component")).toBe(true);
  });
  it("rejects path traversal", () => {
    expect(isValidSlugSegment("..")).toBe(false);
    expect(isValidSlugSegment("a/b")).toBe(false);
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

Run: `pnpm --filter @pretable/app-website test paths.test.ts`
Expected: FAIL — module not found.

- [ ] **Step 3: Implement**

```ts
// apps/website/lib/docs/paths.ts
export interface DocsFrontmatter {
  title: string;
  description: string;
  nav: string;
  order?: number;
}

const SLUG_RE = /^[a-z0-9][a-z0-9-]*$/;

export function isValidSlugSegment(s: string): boolean {
  return SLUG_RE.test(s);
}

const ROOT_INDEX = "getting-started/index.mdx";

export function slugToContentPath(slug: string[]): string {
  if (slug.length === 0) return ROOT_INDEX;
  for (const seg of slug) {
    if (!isValidSlugSegment(seg)) {
      throw new Error(`Invalid slug segment: ${seg}`);
    }
  }
  // Convention: every group dir has index.mdx; sub-pages are flat files.
  // Resolver tries <slug>/index.mdx then <slug>.mdx in load.ts.
  return slug.join("/") + ".mdx";
}

export function contentPathToSlug(p: string): string[] {
  const noExt = p.replace(/\.mdx$/, "");
  const parts = noExt.split("/").filter(Boolean);
  if (parts[parts.length - 1] === "index") parts.pop();
  if (parts.length === 1 && parts[0] === "getting-started") return [];
  return parts;
}
```

- [ ] **Step 4: Run, verify PASS**

Run: `pnpm --filter @pretable/app-website test paths.test.ts`
Expected: 3 describes, all passing.

- [ ] **Step 5: Commit**

```bash
git add apps/website/lib/docs/paths.ts apps/website/lib/docs/__tests__/paths.test.ts
git commit -m "feat(website): docs slug<->path helpers"
```

### Task 2: `extract-headings.ts`

**Files:**

- Create: `apps/website/lib/docs/extract-headings.ts`
- Test: `apps/website/lib/docs/__tests__/extract-headings.test.ts`

- [ ] **Step 1: Write failing tests**

```ts
import { describe, it, expect } from "vitest";
import { extractHeadings } from "../extract-headings";

const SAMPLE = `---
title: x
---
# Title (skipped)

## First section

Body.

### Sub one

### Sub two

## Second section

#### Too deep (skipped)
`;

describe("extractHeadings", () => {
  it("returns H2/H3 only with slugs", () => {
    expect(extractHeadings(SAMPLE)).toEqual([
      { depth: 2, text: "First section", slug: "first-section" },
      { depth: 3, text: "Sub one", slug: "sub-one" },
      { depth: 3, text: "Sub two", slug: "sub-two" },
      { depth: 2, text: "Second section", slug: "second-section" },
    ]);
  });
  it("returns [] when no h2", () => {
    expect(extractHeadings("# Only H1\n\nbody")).toEqual([]);
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

Run: `pnpm --filter @pretable/app-website test extract-headings`
Expected: FAIL.

- [ ] **Step 3: Implement**

````ts
// apps/website/lib/docs/extract-headings.ts
import GithubSlugger from "github-slugger";

export interface DocsHeading {
  depth: 2 | 3;
  text: string;
  slug: string;
}

const HEADING_RE = /^(#{2,3})\s+(.+?)\s*$/gm;
const FENCE_RE = /^```[\s\S]*?^```/gm;
const FRONTMATTER_RE = /^---\n[\s\S]*?\n---\n/;

export function extractHeadings(raw: string): DocsHeading[] {
  const stripped = raw.replace(FRONTMATTER_RE, "").replace(FENCE_RE, "");
  const slugger = new GithubSlugger();
  const out: DocsHeading[] = [];
  for (const m of stripped.matchAll(HEADING_RE)) {
    const depth = m[1].length as 2 | 3;
    const text = m[2].replace(/`/g, "");
    out.push({ depth, text, slug: slugger.slug(text) });
  }
  return out;
}
````

- [ ] **Step 4: Add `github-slugger` dep**

```bash
pnpm --filter @pretable/app-website add github-slugger
```

- [ ] **Step 5: Run, verify PASS**

Run: `pnpm --filter @pretable/app-website test extract-headings`
Expected: 2/2 pass.

- [ ] **Step 6: Commit**

```bash
git add apps/website/lib/docs/extract-headings.ts apps/website/lib/docs/__tests__/extract-headings.test.ts apps/website/package.json pnpm-lock.yaml
git commit -m "feat(website): extract H2/H3 headings from MDX"
```

### Task 3: `enumerate.ts`

**Files:**

- Create: `apps/website/lib/docs/enumerate.ts`
- Test: `apps/website/lib/docs/__tests__/enumerate.test.ts`
- Test fixtures: `apps/website/lib/docs/__tests__/__fixtures__/content/docs/{group/index.mdx,group/page.mdx}`

- [ ] **Step 1: Write fixtures**

```bash
mkdir -p apps/website/lib/docs/__tests__/__fixtures__/content/docs/alpha
```

`apps/website/lib/docs/__tests__/__fixtures__/content/docs/alpha/index.mdx`:

```mdx
---
title: Alpha
description: Alpha overview
nav: Alpha
---

# Alpha
```

`apps/website/lib/docs/__tests__/__fixtures__/content/docs/alpha/one.mdx`:

```mdx
---
title: One
description: First page
nav: Alpha
order: 1
---

# One
```

- [ ] **Step 2: Write failing test**

```ts
// apps/website/lib/docs/__tests__/enumerate.test.ts
import { describe, it, expect } from "vitest";
import path from "node:path";
import { enumerateDocs } from "../enumerate";

const ROOT = path.join(__dirname, "__fixtures__/content/docs");

describe("enumerateDocs", () => {
  it("returns slug + frontmatter for every .mdx file", async () => {
    const pages = await enumerateDocs(ROOT);
    expect(pages).toHaveLength(2);
    expect(pages.map((p) => p.slug)).toEqual([["alpha"], ["alpha", "one"]]);
    expect(pages[0].frontmatter.title).toBe("Alpha");
  });
});
```

- [ ] **Step 3: Run, verify FAIL**

Run: `pnpm --filter @pretable/app-website test enumerate`
Expected: FAIL.

- [ ] **Step 4: Implement**

```ts
// apps/website/lib/docs/enumerate.ts
import fs from "node:fs/promises";
import path from "node:path";
import matter from "gray-matter";
import type { DocsFrontmatter } from "./paths";
import { contentPathToSlug } from "./paths";

export interface DocsPageEntry {
  slug: string[];
  filePath: string;
  frontmatter: DocsFrontmatter;
}

async function walk(dir: string, acc: string[] = []): Promise<string[]> {
  for (const e of await fs.readdir(dir, { withFileTypes: true })) {
    const full = path.join(dir, e.name);
    if (e.isDirectory()) await walk(full, acc);
    else if (e.isFile() && full.endsWith(".mdx")) acc.push(full);
  }
  return acc;
}

export async function enumerateDocs(root: string): Promise<DocsPageEntry[]> {
  const files = await walk(root);
  const out: DocsPageEntry[] = [];
  for (const f of files) {
    const raw = await fs.readFile(f, "utf8");
    const { data } = matter(raw);
    const rel = path.relative(root, f);
    out.push({
      slug: contentPathToSlug(rel),
      filePath: f,
      frontmatter: data as DocsFrontmatter,
    });
  }
  out.sort((a, b) => a.slug.join("/").localeCompare(b.slug.join("/")));
  return out;
}
```

- [ ] **Step 5: Run, verify PASS**

Run: `pnpm --filter @pretable/app-website test enumerate`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add apps/website/lib/docs/enumerate.ts apps/website/lib/docs/__tests__/
git commit -m "feat(website): enumerate docs content tree"
```

### Task 4: `prev-next.ts`

**Files:**

- Create: `apps/website/lib/docs/prev-next.ts`
- Test: `apps/website/lib/docs/__tests__/prev-next.test.ts`

- [ ] **Step 1: Write failing test**

```ts
import { describe, it, expect } from "vitest";
import { resolvePrevNext } from "../prev-next";

const NAV = [
  {
    title: "G1",
    items: [
      { title: "A", href: "/docs/a" },
      { title: "B", href: "/docs/b" },
    ],
  },
  { title: "G2", items: [{ title: "C", href: "/docs/c" }] },
];

describe("resolvePrevNext", () => {
  it("first page has no prev, has next", () => {
    expect(resolvePrevNext("/docs/a", NAV)).toEqual({
      prev: null,
      next: { title: "B", href: "/docs/b" },
    });
  });
  it("middle page has both", () => {
    expect(resolvePrevNext("/docs/b", NAV)).toEqual({
      prev: { title: "A", href: "/docs/a" },
      next: { title: "C", href: "/docs/c" },
    });
  });
  it("last page has prev, no next", () => {
    expect(resolvePrevNext("/docs/c", NAV)).toEqual({
      prev: { title: "B", href: "/docs/b" },
      next: null,
    });
  });
  it("unknown href returns nulls", () => {
    expect(resolvePrevNext("/docs/zzz", NAV)).toEqual({
      prev: null,
      next: null,
    });
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

Run: `pnpm --filter @pretable/app-website test prev-next`

- [ ] **Step 3: Implement**

```ts
// apps/website/lib/docs/prev-next.ts
import type { DocsNavSection, DocsNavItem } from "../../app/docs/_nav";

export interface PrevNext {
  prev: DocsNavItem | null;
  next: DocsNavItem | null;
}

export function resolvePrevNext(
  currentHref: string,
  nav: DocsNavSection[],
): PrevNext {
  const flat: DocsNavItem[] = nav.flatMap((g) => g.items);
  const i = flat.findIndex((it) => it.href === currentHref);
  if (i === -1) return { prev: null, next: null };
  return {
    prev: i > 0 ? flat[i - 1] : null,
    next: i < flat.length - 1 ? flat[i + 1] : null,
  };
}
```

- [ ] **Step 4: Run, verify PASS**

Run: `pnpm --filter @pretable/app-website test prev-next`

- [ ] **Step 5: Commit**

```bash
git add apps/website/lib/docs/prev-next.ts apps/website/lib/docs/__tests__/prev-next.test.ts
git commit -m "feat(website): prev/next neighbor resolution"
```

### Task 5: `define-example.ts`

**Files:**

- Create: `apps/website/lib/docs/define-example.ts`
- Test: `apps/website/lib/docs/__tests__/define-example.test.tsx`

- [ ] **Step 1: Write failing test**

```tsx
import { describe, it, expect } from "vitest";
import { defineExample } from "../define-example";

describe("defineExample", () => {
  it("returns the input narrowed to ExampleDef", () => {
    const def = defineExample({
      title: "X",
      Demo: <div>demo</div>,
      files: [{ path: "page.tsx", lang: "tsx", source: "const x = 1;" }],
    });
    expect(def.title).toBe("X");
    expect(def.files[0].path).toBe("page.tsx");
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

- [ ] **Step 3: Implement**

```ts
// apps/website/lib/docs/define-example.ts
import type { ReactNode } from "react";

export type ExampleLang = "ts" | "tsx" | "js" | "jsx" | "css" | "json" | "bash";

export interface ExampleFile {
  path: string;
  lang: ExampleLang;
  source: string;
}

export interface ExampleDef {
  title: string;
  Demo: ReactNode;
  files: readonly ExampleFile[];
}

export function defineExample(def: ExampleDef): ExampleDef {
  return def;
}
```

- [ ] **Step 4: Run, verify PASS**

- [ ] **Step 5: Commit**

```bash
git add apps/website/lib/docs/define-example.ts apps/website/lib/docs/__tests__/define-example.test.tsx
git commit -m "feat(website): defineExample helper"
```

---

## Phase 2: MDX pipeline

### Task 6: `load.ts` — compileMDX wrapper

**Files:**

- Create: `apps/website/lib/docs/load.ts`
- Test: `apps/website/lib/docs/__tests__/load.test.tsx`

- [ ] **Step 1: Write failing test**

```tsx
import { describe, it, expect } from "vitest";
import path from "node:path";
import { loadDocsPage } from "../load";

const ROOT = path.join(__dirname, "__fixtures__/content/docs");

describe("loadDocsPage", () => {
  it("loads frontmatter and raw source", async () => {
    const r = await loadDocsPage(["alpha", "one"], { root: ROOT });
    expect(r.frontmatter.title).toBe("One");
    expect(r.raw).toContain("# One");
    expect(r.headings).toEqual([]);
  });
  it("falls back to index.mdx for group slug", async () => {
    const r = await loadDocsPage(["alpha"], { root: ROOT });
    expect(r.frontmatter.title).toBe("Alpha");
  });
  it("throws on missing", async () => {
    await expect(loadDocsPage(["nope"], { root: ROOT })).rejects.toThrow();
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

- [ ] **Step 3: Implement**

```ts
// apps/website/lib/docs/load.ts
import fs from "node:fs/promises";
import path from "node:path";
import { compileMDX } from "next-mdx-remote/rsc";
import remarkGfm from "remark-gfm";
import rehypeSlug from "rehype-slug";
import rehypePrettyCode from "rehype-pretty-code";
import type { DocsFrontmatter } from "./paths";
import { extractHeadings, type DocsHeading } from "./extract-headings";
import { docsMdxComponents } from "../../app/components/docs/MdxRenderer";

const DEFAULT_ROOT = path.join(process.cwd(), "content/docs");

export interface LoadOptions {
  root?: string;
}

export interface LoadResult {
  content: React.ReactElement;
  frontmatter: DocsFrontmatter;
  raw: string;
  headings: DocsHeading[];
}

async function resolveFile(root: string, slug: string[]): Promise<string> {
  const base = slug.length === 0 ? "getting-started" : slug.join("/");
  const candidates = [
    path.join(root, `${base}.mdx`),
    path.join(root, base, "index.mdx"),
  ];
  for (const c of candidates) {
    try {
      await fs.access(c);
      return c;
    } catch {}
  }
  throw new Error(`Docs page not found for slug: ${slug.join("/")}`);
}

export async function loadDocsPage(
  slug: string[],
  opts: LoadOptions = {},
): Promise<LoadResult> {
  const root = opts.root ?? DEFAULT_ROOT;
  const file = await resolveFile(root, slug);
  const raw = await fs.readFile(file, "utf8");
  const { content, frontmatter } = await compileMDX<DocsFrontmatter>({
    source: raw,
    options: {
      parseFrontmatter: true,
      mdxOptions: {
        remarkPlugins: [remarkGfm],
        rehypePlugins: [
          rehypeSlug,
          [rehypePrettyCode, { theme: "github-light" }],
        ],
      },
    },
    components: docsMdxComponents,
  });
  return { content, frontmatter, raw, headings: extractHeadings(raw) };
}
```

- [ ] **Step 4: Stub `MdxRenderer` (filled in Task 7)**

`apps/website/app/components/docs/MdxRenderer.tsx`:

```tsx
import type { MDXComponents } from "mdx/types";

export const docsMdxComponents: MDXComponents = {};
```

- [ ] **Step 5: Run, verify PASS**

Run: `pnpm --filter @pretable/app-website test load`
Expected: 3/3.

- [ ] **Step 6: Commit**

```bash
git add apps/website/lib/docs/load.ts apps/website/lib/docs/__tests__/load.test.tsx apps/website/app/components/docs/MdxRenderer.tsx
git commit -m "feat(website): MDX compileMDX wrapper with frontmatter + headings"
```

### Task 7: `MdxRenderer` + `CodeBlock` `<pre>` override

**Files:**

- Modify: `apps/website/app/components/docs/MdxRenderer.tsx`
- Create: `apps/website/app/components/docs/mdx/CodeBlock.tsx`
- Test: `apps/website/app/components/docs/mdx/__tests__/CodeBlock.test.tsx`

- [ ] **Step 1: Write failing CodeBlock test**

```tsx
import { describe, it, expect } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";
import { CodeBlock } from "../CodeBlock";

describe("CodeBlock", () => {
  it("renders children pre and a copy button", () => {
    render(
      <CodeBlock raw="const x = 1;">
        <code>const x = 1;</code>
      </CodeBlock>,
    );
    expect(screen.getByRole("button", { name: /copy/i })).toBeInTheDocument();
  });
  it("writes raw to clipboard on click", async () => {
    const writeText = vi.fn().mockResolvedValue(undefined);
    Object.assign(navigator, { clipboard: { writeText } });
    render(
      <CodeBlock raw="hello">
        <code>hello</code>
      </CodeBlock>,
    );
    fireEvent.click(screen.getByRole("button", { name: /copy/i }));
    expect(writeText).toHaveBeenCalledWith("hello");
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

- [ ] **Step 3: Implement CodeBlock**

```tsx
// apps/website/app/components/docs/mdx/CodeBlock.tsx
"use client";
import { useState, type ReactNode } from "react";

export interface CodeBlockProps {
  children: ReactNode;
  raw: string;
  filename?: string;
  lang?: string;
}

export function CodeBlock({ children, raw, filename, lang }: CodeBlockProps) {
  const [copied, setCopied] = useState(false);
  const onCopy = async () => {
    await navigator.clipboard.writeText(raw);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  };
  return (
    <figure className="relative my-6 rounded-md border border-rule bg-bg-card">
      {(filename || lang) && (
        <figcaption className="flex items-center justify-between border-b border-rule px-3 py-1.5 font-mono text-[11px] text-text-dim">
          <span>{filename ?? ""}</span>
          {lang && <span className="uppercase tracking-[0.1em]">{lang}</span>}
        </figcaption>
      )}
      <button
        type="button"
        aria-label="Copy code"
        onClick={onCopy}
        className="absolute right-2 top-2 rounded-[3px] border border-rule bg-bg-card px-2 py-1 font-mono text-[10px] text-text-secondary hover:text-text-primary"
      >
        {copied ? "Copied" : "Copy"}
      </button>
      <div className="overflow-x-auto px-4 py-3 font-mono text-[13px] leading-[1.55]">
        {children}
      </div>
    </figure>
  );
}
```

- [ ] **Step 4: Wire into MdxRenderer**

```tsx
// apps/website/app/components/docs/MdxRenderer.tsx
import type { MDXComponents } from "mdx/types";
import { CodeBlock } from "./mdx/CodeBlock";

interface PreProps {
  children: React.ReactElement<{
    children?: string;
    className?: string;
    "data-language"?: string;
  }>;
  "data-rehype-pretty-code-figure"?: string;
}

function Pre(props: PreProps) {
  const codeProps = props.children.props;
  const raw = typeof codeProps.children === "string" ? codeProps.children : "";
  const lang = codeProps["data-language"];
  // file titles parsed in a later pass via rehype meta — leave undefined here
  return (
    <CodeBlock raw={raw} lang={lang}>
      {props.children}
    </CodeBlock>
  );
}

export const docsMdxComponents: MDXComponents = {
  pre: Pre as unknown as MDXComponents["pre"],
};
```

- [ ] **Step 5: Run CodeBlock tests, verify PASS**

Run: `pnpm --filter @pretable/app-website test CodeBlock`

- [ ] **Step 6: Commit**

```bash
git add apps/website/app/components/docs/MdxRenderer.tsx apps/website/app/components/docs/mdx/CodeBlock.tsx apps/website/app/components/docs/mdx/__tests__/CodeBlock.test.tsx
git commit -m "feat(website): docs CodeBlock with copy button"
```

---

## Phase 3: New routing

### Task 8: Catch-all `[[...slug]]/page.tsx` for one fixture page

**Files:**

- Create: `apps/website/content/docs/getting-started/index.mdx` (smoke fixture)
- Create: `apps/website/app/docs/[[...slug]]/page.tsx`
- Test: `apps/website/app/docs/[[...slug]]/__tests__/page.test.tsx`

- [ ] **Step 1: Create smoke MDX**

```mdx
---
title: Install + first grid
description: Get a Pretable grid rendering in five minutes.
nav: Getting Started
order: 1
---

# Install + first grid

Body text.

## Install

`pnpm add @pretable/react`.
```

- [ ] **Step 2: Write failing test**

```tsx
// apps/website/app/docs/[[...slug]]/__tests__/page.test.tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import Page from "../page";

describe("docs catch-all page", () => {
  it("renders frontmatter title for empty slug", async () => {
    const ui = await Page({ params: Promise.resolve({ slug: undefined }) });
    render(ui);
    expect(
      screen.getByRole("heading", { level: 1, name: /Install \+ first grid/ }),
    ).toBeInTheDocument();
  });
});
```

- [ ] **Step 3: Run, verify FAIL**

- [ ] **Step 4: Implement page**

```tsx
// apps/website/app/docs/[[...slug]]/page.tsx
import { notFound } from "next/navigation";
import { loadDocsPage } from "../../../lib/docs/load";
import { DocsPageHeader } from "../../components/docs/DocsPageHeader";

interface Params {
  slug?: string[];
}

export default async function Page({ params }: { params: Promise<Params> }) {
  const { slug = [] } = await params;
  let result;
  try {
    result = await loadDocsPage(slug);
  } catch {
    notFound();
  }
  const { content, frontmatter } = result;
  return (
    <article className="docs-prose">
      <DocsPageHeader
        title={frontmatter.title}
        description={frontmatter.description}
      />
      {content}
    </article>
  );
}
```

- [ ] **Step 5: Stub `DocsPageHeader`**

```tsx
// apps/website/app/components/docs/DocsPageHeader.tsx
export function DocsPageHeader({
  title,
  description,
}: {
  title: string;
  description: string;
}) {
  return (
    <header className="mb-8">
      <h1 className="font-display text-[36px] leading-[1.05] tracking-[-0.025em] text-text-primary">
        {title}
      </h1>
      <p className="mt-2 font-display text-[15px] leading-[1.55] text-text-secondary">
        {description}
      </p>
    </header>
  );
}
```

- [ ] **Step 6: Run test, PASS**

- [ ] **Step 7: Browser smoke**

```bash
pnpm --filter @pretable/app-website dev
```

Visit `http://localhost:3000/docs`. Expect H1 "Install + first grid".

- [ ] **Step 8: Commit**

```bash
git add apps/website/content/ apps/website/app/docs/[[...slug]]/ apps/website/app/components/docs/DocsPageHeader.tsx
git commit -m "feat(website): docs catch-all route renders MDX"
```

### Task 9: Middleware `.md` rewrite + `format=md` branch

**Files:**

- Create: `apps/website/middleware.ts`
- Modify: `apps/website/app/docs/[[...slug]]/page.tsx`
- Test: `apps/website/lib/docs/__tests__/raw-response.test.ts`

- [ ] **Step 1: Write failing test for raw response helper**

```ts
import { describe, it, expect } from "vitest";
import { buildRawMarkdownResponse } from "../raw-response";

describe("buildRawMarkdownResponse", () => {
  it("composes title + description + raw body", async () => {
    const r = buildRawMarkdownResponse({
      frontmatter: { title: "T", description: "D", nav: "Getting Started" },
      raw: "---\ntitle: T\n---\nbody",
    });
    expect(r.headers.get("content-type")).toMatch(/text\/markdown/);
    expect(await r.text()).toBe("# T\n\nD\n\nbody");
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

- [ ] **Step 3: Implement helper**

```ts
// apps/website/lib/docs/raw-response.ts
import type { DocsFrontmatter } from "./paths";

const FRONTMATTER_RE = /^---\n[\s\S]*?\n---\n?/;

export function buildRawMarkdownResponse(args: {
  frontmatter: DocsFrontmatter;
  raw: string;
}): Response {
  const body = args.raw.replace(FRONTMATTER_RE, "");
  const text = `# ${args.frontmatter.title}\n\n${args.frontmatter.description}\n\n${body}`;
  return new Response(text, {
    headers: { "Content-Type": "text/markdown; charset=utf-8" },
  });
}
```

- [ ] **Step 4: Implement middleware**

```ts
// apps/website/middleware.ts
import { NextResponse, type NextRequest } from "next/server";

export const config = { matcher: "/docs/:path*" };

export function middleware(req: NextRequest) {
  const url = req.nextUrl.clone();
  if (url.pathname.endsWith(".md")) {
    url.pathname = url.pathname.replace(/\.md$/, "");
    url.searchParams.set("format", "md");
    return NextResponse.rewrite(url);
  }
}
```

- [ ] **Step 5: Branch in page.tsx**

```tsx
// apps/website/app/docs/[[...slug]]/page.tsx
import { notFound } from "next/navigation";
import { loadDocsPage } from "../../../lib/docs/load";
import { buildRawMarkdownResponse } from "../../../lib/docs/raw-response";
import { DocsPageHeader } from "../../components/docs/DocsPageHeader";

interface Params {
  slug?: string[];
}
interface Search {
  format?: string;
}

export default async function Page({
  params,
  searchParams,
}: {
  params: Promise<Params>;
  searchParams: Promise<Search>;
}) {
  const { slug = [] } = await params;
  const { format } = await searchParams;
  let result;
  try {
    result = await loadDocsPage(slug);
  } catch {
    notFound();
  }
  if (format === "md") {
    return buildRawMarkdownResponse({
      frontmatter: result.frontmatter,
      raw: result.raw,
    });
  }
  return (
    <article className="docs-prose">
      <DocsPageHeader
        title={result.frontmatter.title}
        description={result.frontmatter.description}
      />
      {result.content}
    </article>
  );
}
```

- [ ] **Step 6: Run test + smoke**

Run: `pnpm --filter @pretable/app-website test raw-response`
Expected: PASS.

Browser: visit `http://localhost:3000/docs.md`. Expect raw markdown response.

- [ ] **Step 7: Commit**

```bash
git add apps/website/middleware.ts apps/website/lib/docs/raw-response.ts apps/website/lib/docs/__tests__/raw-response.test.ts apps/website/app/docs/[[...slug]]/page.tsx
git commit -m "feat(website): /docs/<slug>.md returns raw markdown"
```

---

## Phase 4: Layout shell

### Task 10: `DocsShell` 3-column layout + replace `app/docs/layout.tsx`

**Files:**

- Create: `apps/website/app/components/docs/DocsShell.tsx`
- Modify: `apps/website/app/docs/layout.tsx`
- Create: `apps/website/app/components/docs/DocsTOC.tsx` (placeholder, filled in Task 14)
- Test: `apps/website/app/components/docs/__tests__/DocsShell.test.tsx`

- [ ] **Step 1: Write failing shell test**

```tsx
import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import { DocsShell } from "../DocsShell";

describe("DocsShell", () => {
  it("renders sidebar, main, toc slots", () => {
    render(
      <DocsShell sidebar={<div>SIDEBAR</div>} toc={<div>TOC</div>}>
        <div>MAIN</div>
      </DocsShell>,
    );
    expect(screen.getByText("SIDEBAR")).toBeInTheDocument();
    expect(screen.getByText("MAIN")).toBeInTheDocument();
    expect(screen.getByText("TOC")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

- [ ] **Step 3: Implement DocsShell**

```tsx
// apps/website/app/components/docs/DocsShell.tsx
import type { ReactNode } from "react";

export interface DocsShellProps {
  sidebar: ReactNode;
  toc: ReactNode | null;
  children: ReactNode;
}

export function DocsShell({ sidebar, toc, children }: DocsShellProps) {
  return (
    <div className="mx-auto w-full max-w-[1440px] px-5 md:px-6 lg:px-8">
      <div className="grid gap-8 md:grid-cols-[200px_minmax(0,1fr)] lg:grid-cols-[240px_minmax(0,1fr)] xl:grid-cols-[260px_minmax(0,1fr)_220px] xl:gap-12">
        <aside className="hidden md:block">
          <div className="sticky top-11 max-h-[calc(100vh-44px)] overflow-y-auto py-8">
            {sidebar}
          </div>
        </aside>
        <main className="min-w-0 py-8">
          <div className="mx-auto max-w-[72ch]">{children}</div>
        </main>
        {toc && (
          <aside className="hidden xl:block">
            <div className="sticky top-11 max-h-[calc(100vh-44px)] overflow-y-auto py-8">
              {toc}
            </div>
          </aside>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: TOC placeholder**

```tsx
// apps/website/app/components/docs/DocsTOC.tsx
import type { DocsHeading } from "../../../lib/docs/extract-headings";

export function DocsTOC({ headings }: { headings: DocsHeading[] }) {
  if (headings.length === 0) return null;
  return (
    <nav aria-labelledby="toc-label">
      <p
        id="toc-label"
        className="mb-3 font-mono text-[11px] uppercase tracking-[0.14em] text-text-dim"
      >
        On this page
      </p>
      <ul className="flex flex-col gap-1">
        {headings.map((h) => (
          <li key={h.slug} className={h.depth === 3 ? "pl-4" : ""}>
            <a
              className="text-[12.5px] text-text-secondary hover:text-text-primary"
              href={`#${h.slug}`}
            >
              {h.text}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  );
}
```

- [ ] **Step 5: Replace layout.tsx**

```tsx
// apps/website/app/docs/layout.tsx
import { NavBar } from "../components/NavBar";

export default function DocsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      <NavBar mode="site" />
      {children}
    </>
  );
}
```

(Layout no longer wraps in DocsShell — page.tsx now does, because each page may have a different TOC.)

- [ ] **Step 6: Wire DocsShell into page.tsx**

```tsx
// apps/website/app/docs/[[...slug]]/page.tsx (additions)
import { DocsShell } from "../../components/docs/DocsShell";
import { DocsSidebar } from "../../components/docs/DocsSidebar";
import { DocsTOC } from "../../components/docs/DocsTOC";

// inside the HTML branch:
return (
  <DocsShell
    sidebar={<DocsSidebar />}
    toc={<DocsTOC headings={result.headings} />}
  >
    <article className="docs-prose">
      <DocsPageHeader
        title={result.frontmatter.title}
        description={result.frontmatter.description}
      />
      {result.content}
    </article>
  </DocsShell>
);
```

- [ ] **Step 7: Stub DocsSidebar (Task 12 fills it)**

```tsx
// apps/website/app/components/docs/DocsSidebar.tsx
export function DocsSidebar() {
  return <nav>sidebar (todo)</nav>;
}
```

- [ ] **Step 8: Run tests + browser smoke**

Run: `pnpm --filter @pretable/app-website test DocsShell`
Browser: `/docs` — three columns at xl, two below 1280, one on mobile.

- [ ] **Step 9: Commit**

```bash
git add apps/website/app/components/docs/ apps/website/app/docs/[[...slug]]/page.tsx apps/website/app/docs/layout.tsx
git commit -m "feat(website): docs 3-column shell layout"
```

### Task 11: `DocsBreadcrumb`

**Files:**

- Create: `apps/website/app/components/docs/DocsBreadcrumb.tsx`
- Test: `apps/website/app/components/docs/__tests__/DocsBreadcrumb.test.tsx`

- [ ] **Step 1: Failing test**

```tsx
import { render, screen } from "@testing-library/react";
import { DocsBreadcrumb } from "../DocsBreadcrumb";

describe("DocsBreadcrumb", () => {
  it("renders group › title", () => {
    render(<DocsBreadcrumb group="Grid" title="<Pretable> component" />);
    expect(screen.getByText(/Grid/)).toBeInTheDocument();
    expect(screen.getByText(/<Pretable> component/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

- [ ] **Step 3: Implement**

```tsx
// apps/website/app/components/docs/DocsBreadcrumb.tsx
export function DocsBreadcrumb({
  group,
  title,
}: {
  group: string;
  title: string;
}) {
  return (
    <p className="font-mono text-[11px] uppercase tracking-[0.14em] text-text-dim">
      {group} <span aria-hidden="true">›</span>{" "}
      <span className="text-text-secondary normal-case tracking-normal">
        {title}
      </span>
    </p>
  );
}
```

- [ ] **Step 4: Add to DocsPageHeader**

```tsx
// apps/website/app/components/docs/DocsPageHeader.tsx
import { DocsBreadcrumb } from "./DocsBreadcrumb";

export function DocsPageHeader({
  group,
  title,
  description,
}: {
  group: string;
  title: string;
  description: string;
}) {
  return (
    <header className="mb-8">
      <DocsBreadcrumb group={group} title={title} />
      <h1 className="mt-3 font-display text-[36px] leading-[1.05] tracking-[-0.025em] text-text-primary">
        {title}
      </h1>
      <p className="mt-3 font-display text-[15px] leading-[1.55] text-text-secondary">
        {description}
      </p>
    </header>
  );
}
```

- [ ] **Step 5: Pass `group` from page.tsx**

```tsx
<DocsPageHeader
  group={result.frontmatter.nav}
  title={result.frontmatter.title}
  description={result.frontmatter.description}
/>
```

- [ ] **Step 6: Run test, PASS, commit**

```bash
git add apps/website/app/components/docs/DocsBreadcrumb.tsx apps/website/app/components/docs/__tests__/DocsBreadcrumb.test.tsx apps/website/app/components/docs/DocsPageHeader.tsx apps/website/app/docs/[[...slug]]/page.tsx
git commit -m "feat(website): docs breadcrumb"
```

---

## Phase 5: Sidebar

### Task 12: `DocsSidebar` — sticky, scrollable, active state

**Files:**

- Modify: `apps/website/app/components/docs/DocsSidebar.tsx`
- Test: `apps/website/app/components/docs/__tests__/DocsSidebar.test.tsx`

- [ ] **Step 1: Failing test**

```tsx
import { render, screen } from "@testing-library/react";
import { vi } from "vitest";
vi.mock("next/navigation", () => ({
  usePathname: () => "/docs/grid/pretable-component",
}));
import { DocsSidebar } from "../DocsSidebar";

describe("DocsSidebar", () => {
  it("renders all _nav.ts groups", () => {
    render(<DocsSidebar />);
    expect(screen.getByText(/Getting Started/i)).toBeInTheDocument();
    expect(screen.getByText(/Grid/i)).toBeInTheDocument();
    expect(screen.getByText(/Streaming/i)).toBeInTheDocument();
    expect(screen.getByText(/Theming/i)).toBeInTheDocument();
  });
  it("marks active link with aria-current=page", () => {
    render(<DocsSidebar />);
    expect(
      screen.getByRole("link", { name: /<Pretable> component/i }),
    ).toHaveAttribute("aria-current", "page");
  });
  it("does not mark sibling group entries as active", () => {
    render(<DocsSidebar />);
    expect(
      screen.getByRole("link", { name: /API reference/i }),
    ).not.toHaveAttribute("aria-current");
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

- [ ] **Step 3: Implement**

```tsx
// apps/website/app/components/docs/DocsSidebar.tsx
"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { docsNav } from "../../docs/_nav";

export function DocsSidebar() {
  const pathname = usePathname();
  return (
    <nav aria-label="Docs sections" className="flex flex-col">
      {docsNav.map((group) => (
        <div key={group.title} className="pt-5 first:pt-0">
          <p className="px-3 pb-2 font-mono text-[11px] uppercase tracking-[0.14em] text-text-dim">
            {group.title}
          </p>
          <ul className="flex flex-col">
            {group.items.map((item) => {
              const active = pathname === item.href;
              return (
                <li key={item.href}>
                  <Link
                    href={item.href}
                    aria-current={active ? "page" : undefined}
                    className={[
                      "block py-1.5 pl-3 text-[13px] border-l",
                      active
                        ? "border-l-2 -ml-px border-l-accent text-accent font-medium"
                        : "border-l-rule-soft text-text-secondary hover:text-text-primary hover:border-l-rule",
                    ].join(" ")}
                  >
                    {item.title}
                  </Link>
                </li>
              );
            })}
          </ul>
        </div>
      ))}
    </nav>
  );
}
```

- [ ] **Step 4: Run test, PASS**

- [ ] **Step 5: Browser smoke**

Visit `/docs/grid/pretable-component` (will 404 until that content exists; substitute with `/docs` and verify Getting Started is active). Sidebar groups all visible, active link styled.

- [ ] **Step 6: Commit**

```bash
git add apps/website/app/components/docs/DocsSidebar.tsx apps/website/app/components/docs/__tests__/DocsSidebar.test.tsx
git commit -m "feat(website): docs sidebar with active state"
```

### Task 13: Mobile drawer

**Files:**

- Create: `apps/website/app/components/docs/DocsMobileDrawer.tsx`
- Modify: `apps/website/app/components/docs/DocsShell.tsx`
- Test: `apps/website/app/components/docs/__tests__/DocsMobileDrawer.test.tsx`

- [ ] **Step 1: Failing test**

```tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { DocsMobileDrawer } from "../DocsMobileDrawer";

describe("DocsMobileDrawer", () => {
  it("opens via button, closes via Esc", () => {
    render(
      <DocsMobileDrawer>
        <div>content</div>
      </DocsMobileDrawer>,
    );
    const btn = screen.getByRole("button", { name: /menu/i });
    fireEvent.click(btn);
    expect(screen.getByRole("dialog")).toBeInTheDocument();
    fireEvent.keyDown(document, { key: "Escape" });
    expect(screen.queryByRole("dialog")).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

- [ ] **Step 3: Implement**

```tsx
// apps/website/app/components/docs/DocsMobileDrawer.tsx
"use client";
import { useEffect, useState, type ReactNode } from "react";

export function DocsMobileDrawer({ children }: { children: ReactNode }) {
  const [open, setOpen] = useState(false);
  useEffect(() => {
    if (!open) return;
    const onKey = (e: KeyboardEvent) => {
      if (e.key === "Escape") setOpen(false);
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [open]);
  return (
    <>
      <button
        type="button"
        aria-label="Menu"
        onClick={() => setOpen(true)}
        className="fixed bottom-4 left-4 z-40 rounded-md border border-rule bg-bg-card px-3 py-2 font-mono text-[12px] shadow md:hidden"
      >
        Menu
      </button>
      {open && (
        <div
          className="fixed inset-0 z-50 md:hidden"
          role="dialog"
          aria-modal="true"
        >
          <div
            className="absolute inset-0 bg-black/40"
            onClick={() => setOpen(false)}
          />
          <div className="absolute left-0 top-0 h-full w-72 overflow-y-auto border-r border-rule bg-bg-card p-6">
            <button
              type="button"
              aria-label="Close"
              onClick={() => setOpen(false)}
              className="mb-4 font-mono text-[11px] text-text-dim"
            >
              ✕ Close
            </button>
            <div onClick={() => setOpen(false)}>{children}</div>
          </div>
        </div>
      )}
    </>
  );
}
```

- [ ] **Step 4: Wire into DocsShell**

In `DocsShell.tsx`, render `<DocsMobileDrawer>{sidebar}</DocsMobileDrawer>` (component renders nothing on `md:` so it's safe to always include).

- [ ] **Step 5: Run test, PASS**

- [ ] **Step 6: Commit**

```bash
git add apps/website/app/components/docs/DocsMobileDrawer.tsx apps/website/app/components/docs/__tests__/DocsMobileDrawer.test.tsx apps/website/app/components/docs/DocsShell.tsx
git commit -m "feat(website): docs mobile drawer"
```

---

## Phase 6: TOC scrollspy

### Task 14: `DocsTOC` scrollspy enhancement

**Files:**

- Modify: `apps/website/app/components/docs/DocsTOC.tsx`
- Test: `apps/website/app/components/docs/__tests__/DocsTOC.test.tsx`

- [ ] **Step 1: Failing test**

```tsx
import { render, screen } from "@testing-library/react";
import { DocsTOC } from "../DocsTOC";

describe("DocsTOC", () => {
  it("renders headings as links", () => {
    render(<DocsTOC headings={[{ depth: 2, text: "A", slug: "a" }]} />);
    expect(screen.getByRole("link", { name: "A" })).toHaveAttribute(
      "href",
      "#a",
    );
  });
  it("returns null for empty headings", () => {
    const { container } = render(<DocsTOC headings={[]} />);
    expect(container).toBeEmptyDOMElement();
  });
});
```

- [ ] **Step 2: Run, verify FAIL → PASS** (basic render already works from Task 10 stub)

- [ ] **Step 3: Add scrollspy**

```tsx
// apps/website/app/components/docs/DocsTOC.tsx
"use client";
import { useEffect, useState } from "react";
import type { DocsHeading } from "../../../lib/docs/extract-headings";

export function DocsTOC({ headings }: { headings: DocsHeading[] }) {
  const [activeSlug, setActiveSlug] = useState<string | null>(null);

  useEffect(() => {
    if (headings.length === 0) return;
    const els = headings
      .map((h) => document.getElementById(h.slug))
      .filter((el): el is HTMLElement => el !== null);
    if (els.length === 0) return;
    const obs = new IntersectionObserver(
      (entries) => {
        const visible = entries
          .filter((e) => e.isIntersecting)
          .sort((a, b) => a.boundingClientRect.top - b.boundingClientRect.top);
        if (visible[0]) setActiveSlug(visible[0].target.id);
      },
      { rootMargin: "-44px 0px -70% 0px", threshold: 0 },
    );
    els.forEach((el) => obs.observe(el));
    return () => obs.disconnect();
  }, [headings]);

  if (headings.length === 0) return null;
  return (
    <nav aria-labelledby="toc-label">
      <p
        id="toc-label"
        className="mb-3 font-mono text-[11px] uppercase tracking-[0.14em] text-text-dim"
      >
        On this page
      </p>
      <ul className="flex flex-col gap-1">
        {headings.map((h) => {
          const active = h.slug === activeSlug;
          return (
            <li key={h.slug} className={h.depth === 3 ? "pl-4" : ""}>
              <a
                aria-current={active ? "location" : undefined}
                href={`#${h.slug}`}
                className={`text-[12.5px] ${active ? "text-accent" : "text-text-secondary hover:text-text-primary"}`}
              >
                {h.text}
              </a>
            </li>
          );
        })}
      </ul>
    </nav>
  );
}
```

- [ ] **Step 4: Run test PASS**

- [ ] **Step 5: Commit**

```bash
git add apps/website/app/components/docs/DocsTOC.tsx apps/website/app/components/docs/__tests__/DocsTOC.test.tsx
git commit -m "feat(website): TOC scrollspy via IntersectionObserver"
```

---

## Phase 7: MDX components

Each follows the same TDD shape: failing test → implement → register in `MdxRenderer` → commit. Pattern is shown for `Callout`, then summarized for the rest.

### Task 15: `Callout`

**Files:**

- Create: `apps/website/app/components/docs/mdx/Callout.tsx`
- Test: `apps/website/app/components/docs/mdx/__tests__/Callout.test.tsx`
- Modify: `apps/website/app/components/docs/MdxRenderer.tsx`

- [ ] **Step 1: Failing test**

```tsx
import { render, screen } from "@testing-library/react";
import { Callout } from "../Callout";

describe("Callout", () => {
  it("renders children inside a region", () => {
    render(<Callout type="note">hello</Callout>);
    expect(screen.getByRole("note")).toHaveTextContent("hello");
  });
  it("supports tip/warning/info/check types", () => {
    for (const type of ["tip", "warning", "info", "check"] as const) {
      const { unmount } = render(<Callout type={type}>x</Callout>);
      expect(screen.getByRole("note")).toBeInTheDocument();
      unmount();
    }
  });
});
```

- [ ] **Step 2: Run, FAIL**

- [ ] **Step 3: Implement**

```tsx
// apps/website/app/components/docs/mdx/Callout.tsx
import type { ReactNode } from "react";

export type CalloutType = "note" | "tip" | "warning" | "info" | "check";

const STYLES: Record<CalloutType, string> = {
  note: "border-l-text-dim bg-bg-card/50",
  tip: "border-l-accent bg-accent/8",
  warning: "border-l-amber-500 bg-amber-500/8",
  info: "border-l-sky-600 bg-sky-600/8",
  check: "border-l-emerald-600 bg-emerald-600/8",
};

const ICON: Record<CalloutType, string> = {
  note: "✎",
  tip: "★",
  warning: "⚠",
  info: "ℹ",
  check: "✓",
};

export function Callout({
  type = "note",
  children,
}: {
  type?: CalloutType;
  children: ReactNode;
}) {
  return (
    <aside
      role="note"
      className={`my-5 rounded-[4px] border border-rule border-l-2 p-4 text-[14px] ${STYLES[type]}`}
    >
      <div className="flex gap-3">
        <span aria-hidden="true" className="font-mono text-text-dim">
          {ICON[type]}
        </span>
        <div className="prose-tight">{children}</div>
      </div>
    </aside>
  );
}
```

- [ ] **Step 4: Register**

```tsx
// MdxRenderer.tsx (additions)
import { Callout } from "./mdx/Callout";
export const docsMdxComponents: MDXComponents = {
  pre: Pre as unknown as MDXComponents["pre"],
  Callout,
};
```

- [ ] **Step 5: Run test PASS, commit**

```bash
git add apps/website/app/components/docs/mdx/Callout.tsx apps/website/app/components/docs/mdx/__tests__/Callout.test.tsx apps/website/app/components/docs/MdxRenderer.tsx
git commit -m "feat(website): Callout MDX component"
```

### Task 16: `Steps` / `Step`

Same shape. Files: `Steps.tsx` exporting `{ Steps, Step }`.

- [ ] **Step 1: Failing test** — render `<Steps><Step title="A">body</Step><Step title="B">body</Step></Steps>` expects two list items numbered 1 and 2.

- [ ] **Step 2: Implement**

```tsx
// apps/website/app/components/docs/mdx/Steps.tsx
import { Children, type ReactNode, isValidElement } from "react";

export function Steps({ children }: { children: ReactNode }) {
  const items = Children.toArray(children).filter(isValidElement);
  return (
    <ol className="my-6 flex flex-col gap-4 border-l border-rule pl-6">
      {items.map((c, i) => (
        <li key={i} className="relative">
          <span
            aria-hidden="true"
            className="absolute -left-[34px] top-0 flex h-6 w-6 items-center justify-center rounded-full border border-rule bg-bg-card font-mono text-[11px] text-text-secondary"
          >
            {i + 1}
          </span>
          {c}
        </li>
      ))}
    </ol>
  );
}

export function Step({
  title,
  children,
}: {
  title: string;
  children: ReactNode;
}) {
  return (
    <div>
      <h4 className="font-display text-[15px] text-text-primary">{title}</h4>
      <div className="mt-1 text-[14px] text-text-secondary">{children}</div>
    </div>
  );
}
```

- [ ] **Step 3: Register both in MdxRenderer.**

- [ ] **Step 4: Test PASS, commit.**

```bash
git commit -m "feat(website): Steps MDX component"
```

### Task 17: `Tabs` / `Tab`

Follows the same shape. Implementation:

```tsx
// apps/website/app/components/docs/mdx/Tabs.tsx
"use client";
import {
  Children,
  isValidElement,
  useState,
  type ReactElement,
  type ReactNode,
} from "react";

interface TabProps {
  label: string;
  children: ReactNode;
}
export function Tab(_: TabProps): ReactNode {
  return null; /* read by Tabs */
}

export function Tabs({ children }: { children: ReactNode }) {
  const tabs = Children.toArray(children).filter(
    (c): c is ReactElement<TabProps> => isValidElement(c) && c.type === Tab,
  );
  const [active, setActive] = useState(0);
  return (
    <div className="my-6 rounded-md border border-rule">
      <div
        role="tablist"
        className="flex border-b border-rule font-mono text-[12px]"
      >
        {tabs.map((t, i) => (
          <button
            key={i}
            role="tab"
            aria-selected={i === active}
            onClick={() => setActive(i)}
            className={`px-3 py-2 ${i === active ? "text-accent border-b-2 border-b-accent -mb-px" : "text-text-secondary hover:text-text-primary"}`}
          >
            {t.props.label}
          </button>
        ))}
      </div>
      <div role="tabpanel" className="p-4">
        {tabs[active]?.props.children}
      </div>
    </div>
  );
}
```

Test: render two Tabs with different labels, click the second, verify second body shown.

Register, test, commit:

```bash
git commit -m "feat(website): Tabs MDX component"
```

### Task 18: `CodeGroup`

Tabs of code blocks, same UX as Tabs but each child is a fenced `<pre>` with a language label that becomes the tab label. Implementation reuses Tabs internally; child components are raw MDX `<pre>` elements wrapped to extract the language.

```tsx
// apps/website/app/components/docs/mdx/CodeGroup.tsx
"use client";
import {
  Children,
  isValidElement,
  useState,
  type ReactElement,
  type ReactNode,
} from "react";

export function CodeGroup({ children }: { children: ReactNode }) {
  const panels = Children.toArray(children).filter(
    isValidElement,
  ) as ReactElement<{
    "data-language"?: string;
  }>[];
  const [active, setActive] = useState(0);
  return (
    <div className="my-6 rounded-md border border-rule">
      <div
        role="tablist"
        className="flex border-b border-rule font-mono text-[11px]"
      >
        {panels.map((p, i) => (
          <button
            key={i}
            role="tab"
            aria-selected={i === active}
            onClick={() => setActive(i)}
            className={`px-3 py-2 uppercase tracking-[0.1em] ${i === active ? "text-accent" : "text-text-secondary"}`}
          >
            {p.props["data-language"] ?? `tab ${i + 1}`}
          </button>
        ))}
      </div>
      <div>{panels[active]}</div>
    </div>
  );
}
```

Test, register, commit:

```bash
git commit -m "feat(website): CodeGroup MDX component"
```

### Task 19: `Card` / `CardGroup`

```tsx
// apps/website/app/components/docs/mdx/Card.tsx
import Link from "next/link";
import type { ReactNode } from "react";

export function CardGroup({
  cols = 2,
  children,
}: {
  cols?: 1 | 2 | 3;
  children: ReactNode;
}) {
  const grid = { 1: "grid-cols-1", 2: "md:grid-cols-2", 3: "md:grid-cols-3" }[
    cols
  ];
  return (
    <div className={`my-6 grid grid-cols-1 gap-3 ${grid}`}>{children}</div>
  );
}

export function Card({
  title,
  href,
  children,
}: {
  title: string;
  href: string;
  children: ReactNode;
}) {
  return (
    <Link
      href={href}
      className="block rounded-[4px] border border-rule bg-bg-card/50 p-4 hover:border-rule"
    >
      <h4 className="font-display text-[15px] text-text-primary">{title}</h4>
      <p className="mt-1 text-[13px] text-text-secondary">{children}</p>
    </Link>
  );
}
```

Test (group renders right column count, card link navigates), register, commit:

```bash
git commit -m "feat(website): Card / CardGroup MDX components"
```

### Task 20: `Frame`

```tsx
// apps/website/app/components/docs/mdx/Frame.tsx
import type { ReactNode } from "react";
export function Frame({
  caption,
  children,
}: {
  caption?: string;
  children: ReactNode;
}) {
  return (
    <figure className="my-6 rounded-md border border-rule bg-bg-card/40 p-3">
      <div className="overflow-hidden rounded-[4px]">{children}</div>
      {caption && (
        <figcaption className="mt-2 text-center font-mono text-[11px] text-text-dim">
          {caption}
        </figcaption>
      )}
    </figure>
  );
}
```

Test, register, commit:

```bash
git commit -m "feat(website): Frame MDX component"
```

### Task 21: `Prompt` + `CopyPromptButton`

**Files:**

- Create: `apps/website/app/components/docs/CopyPromptButton.tsx`
- Create: `apps/website/app/components/docs/mdx/Prompt.tsx`
- Test for each.

- [ ] **Step 1: Implement CopyPromptButton (same pattern as CodeBlock)**

```tsx
"use client";
import { useState } from "react";
export function CopyPromptButton({ text }: { text: string }) {
  const [copied, setCopied] = useState(false);
  return (
    <button
      type="button"
      onClick={async () => {
        await navigator.clipboard.writeText(text);
        setCopied(true);
        setTimeout(() => setCopied(false), 2000);
      }}
      className="rounded-[3px] border border-rule bg-bg-card px-2 py-1 font-mono text-[10px] text-text-secondary hover:text-text-primary"
    >
      {copied ? "Copied" : "Copy prompt"}
    </button>
  );
}
```

- [ ] **Step 2: Implement Prompt**

```tsx
import { Children, type ReactNode } from "react";
import { CopyPromptButton } from "../CopyPromptButton";

function flattenText(node: ReactNode): string {
  if (typeof node === "string") return node;
  if (Array.isArray(node)) return node.map(flattenText).join("");
  if (node && typeof node === "object" && "props" in node) {
    return flattenText(
      (node as { props?: { children?: ReactNode } }).props?.children,
    );
  }
  return "";
}

export function Prompt({ children }: { children: ReactNode }) {
  const text = flattenText(children).trim();
  return (
    <aside className="my-6 rounded-md border border-rule border-l-2 border-l-accent bg-accent/8 p-4">
      <div className="mb-3 flex items-center justify-between">
        <span className="font-mono text-[11px] uppercase tracking-[0.14em] text-text-dim">
          Prompt
        </span>
        <CopyPromptButton text={text} />
      </div>
      <div className="whitespace-pre-wrap font-mono text-[13px] leading-[1.55] text-text-secondary">
        {children}
      </div>
    </aside>
  );
}
```

- [ ] **Step 3: Register, test, commit**

```bash
git commit -m "feat(website): Prompt + CopyPromptButton"
```

---

## Phase 8: `<Example>` component

### Task 22: First example folder + raw imports

**Files:**

- Create: `apps/website/content/examples/streaming-chat-grid/{ChatGrid.tsx,columns.ts,openai-client.ts,page.tsx,index.ts}`
- Modify: `apps/website/next.config.ts` — confirm `?raw` import works (Turbopack supports it natively in Next 16; if not, add a webpack rule)

- [ ] **Step 1: Create the example files** (use the source from `apps/website/app/components/CodeExample.tsx` `TABS` constant verbatim — same code).

- [ ] **Step 2: Create `index.ts`**

```ts
// apps/website/content/examples/streaming-chat-grid/index.ts
import { defineExample } from "../../../lib/docs/define-example";
import { ChatGrid } from "./ChatGrid";

import pageSource from "./page.tsx?raw";
import chatGridSource from "./ChatGrid.tsx?raw";
import columnsSource from "./columns.ts?raw";
import openaiSource from "./openai-client.ts?raw";

export const streamingChatGrid = defineExample({
  title: "Streaming chat grid",
  Demo: <ChatGrid prompt="Summarize the last 10 incidents" />,
  files: [
    { path: "page.tsx", lang: "tsx", source: pageSource },
    { path: "ChatGrid.tsx", lang: "tsx", source: chatGridSource },
    { path: "columns.ts", lang: "ts", source: columnsSource },
    { path: "openai-client.ts", lang: "ts", source: openaiSource },
  ],
});
```

- [ ] **Step 3: Verify `?raw` import** — `pnpm --filter @pretable/app-website dev`, render in a temp page, console.log first chars. If it fails (Turbopack doesn't recognize): add to `next.config.ts`:

```ts
// next.config.ts
const nextConfig = {
  turbopack: {
    rules: { "*.tsx?raw": { loaders: ["raw-loader"], as: "*.js" } },
  },
};
```

(Adjust to actual API if needed; test in browser.)

- [ ] **Step 4: Commit**

```bash
git add apps/website/content/examples/ apps/website/next.config.ts
git commit -m "feat(website): first live example (streaming-chat-grid)"
```

### Task 23: `<Example>` component

**Files:**

- Create: `apps/website/app/components/docs/mdx/Example.tsx`
- Test: `apps/website/app/components/docs/mdx/__tests__/Example.test.tsx`

- [ ] **Step 1: Failing test**

````tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { Example } from "../Example";
import { defineExample } from "../../../../../lib/docs/define-example";

const def = defineExample({
  title: "Demo",
  Demo: <div>LIVE</div>,
  files: [
    { path: "a.ts", lang: "ts", source: "export const a = 1;" },
    { path: "b.ts", lang: "ts", source: "export const b = 2;" },
  ],
});

describe("Example", () => {
  it("renders live demo by default", () => {
    render(<Example example={def} />);
    expect(screen.getByText("LIVE")).toBeInTheDocument();
  });
  it("show source disclosure reveals tabs", () => {
    render(<Example example={def} />);
    fireEvent.click(screen.getByRole("button", { name: /show source/i }));
    expect(screen.getByRole("tab", { name: "a.ts" })).toBeInTheDocument();
    expect(screen.getByRole("tab", { name: "b.ts" })).toBeInTheDocument();
  });
  it("Copy all writes a fenced bundle", async () => {
    const writeText = vi.fn().mockResolvedValue(undefined);
    Object.assign(navigator, { clipboard: { writeText } });
    render(<Example example={def} defaultOpen />);
    fireEvent.click(screen.getByRole("button", { name: /copy all/i }));
    expect(writeText).toHaveBeenCalledWith(
      "```ts a.ts\nexport const a = 1;\n```\n\n```ts b.ts\nexport const b = 2;\n```\n",
    );
  });
});
````

- [ ] **Step 2: Run, verify FAIL**

- [ ] **Step 3: Implement**

````tsx
// apps/website/app/components/docs/mdx/Example.tsx
"use client";
import { useState } from "react";
import type { ExampleDef } from "../../../../lib/docs/define-example";

export interface ExampleProps {
  example: ExampleDef;
  defaultOpen?: boolean;
  showLive?: boolean;
}

export function Example({
  example,
  defaultOpen = false,
  showLive = true,
}: ExampleProps) {
  const [open, setOpen] = useState(defaultOpen);
  const [active, setActive] = useState(0);
  const file = example.files[active];

  const copyFile = () => navigator.clipboard.writeText(file.source);
  const copyAll = () =>
    navigator.clipboard.writeText(
      example.files
        .map((f) => "```" + f.lang + " " + f.path + "\n" + f.source + "\n```")
        .join("\n\n") + "\n",
    );

  return (
    <figure className="my-6 rounded-md border border-rule">
      {showLive && (
        <div className="border-b border-rule bg-bg-card/40 p-4">
          <div className="mb-2 font-mono text-[11px] uppercase tracking-[0.14em] text-text-dim">
            {example.title} — live
          </div>
          {example.Demo}
        </div>
      )}
      <button
        type="button"
        onClick={() => setOpen((o) => !o)}
        className="flex w-full items-center justify-between border-b border-rule px-3 py-2 font-mono text-[11px] uppercase tracking-[0.14em] text-text-secondary hover:text-text-primary"
      >
        <span>{open ? "▾ Hide source" : "▸ Show source"}</span>
        <span className="text-text-dim">{example.files.length} files</span>
      </button>
      {open && (
        <div>
          <div
            role="tablist"
            className="flex border-b border-rule font-mono text-[11.5px]"
          >
            {example.files.map((f, i) => (
              <button
                key={f.path}
                role="tab"
                aria-selected={i === active}
                onClick={() => setActive(i)}
                className={`px-3 py-2 ${i === active ? "text-accent" : "text-text-secondary hover:text-text-primary"}`}
              >
                {f.path}
              </button>
            ))}
            <div className="ml-auto flex items-center gap-2 px-3">
              <button
                type="button"
                onClick={copyFile}
                className="font-mono text-[10px] text-text-secondary hover:text-text-primary"
              >
                Copy file
              </button>
              <button
                type="button"
                onClick={copyAll}
                className="font-mono text-[10px] text-accent hover:text-accent-deep"
              >
                Copy all
              </button>
            </div>
          </div>
          <pre className="overflow-x-auto px-4 py-3 font-mono text-[12.5px] leading-[1.55]">
            {file.source}
          </pre>
        </div>
      )}
    </figure>
  );
}
````

(Note: source rendered as plain `<pre>` here — to apply Shiki at build, the example loader can pre-highlight to HTML and pass `htmlSource` instead. Defer that polish; current output is readable and the AI clipboard target is what matters.)

- [ ] **Step 4: Register in MdxRenderer**

```tsx
import { Example } from "./mdx/Example";
export const docsMdxComponents: MDXComponents = { /* prior */, Example };
```

- [ ] **Step 5: Run tests, PASS, commit**

```bash
git commit -m "feat(website): <Example> live demo + source disclosure + copy-all"
```

---

## Phase 9: AI features (CopyPageButton, llms.txt, llms-full.txt)

### Task 24: `CopyPageButton` + discovery `<link>`

**Files:**

- Create: `apps/website/app/components/docs/CopyPageButton.tsx`
- Modify: `apps/website/app/components/docs/DocsPageHeader.tsx`
- Modify: `apps/website/app/docs/[[...slug]]/page.tsx` (add `<link rel="alternate">` via Next metadata or in-page `<head>` — Next 16 prefers `generateMetadata`)
- Test: `apps/website/app/components/docs/__tests__/CopyPageButton.test.tsx`

- [ ] **Step 1: Failing test**

```tsx
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { CopyPageButton } from "../CopyPageButton";

describe("CopyPageButton", () => {
  it("fetches <path>.md and writes to clipboard", async () => {
    global.fetch = vi
      .fn()
      .mockResolvedValue({ text: () => Promise.resolve("# hi") });
    const writeText = vi.fn().mockResolvedValue(undefined);
    Object.assign(navigator, { clipboard: { writeText } });
    render(<CopyPageButton path="/docs/grid" />);
    fireEvent.click(screen.getByRole("button", { name: /copy as markdown/i }));
    await waitFor(() => expect(writeText).toHaveBeenCalledWith("# hi"));
  });
});
```

- [ ] **Step 2: Implement**

```tsx
"use client";
import { useState } from "react";

export function CopyPageButton({ path }: { path: string }) {
  const [copied, setCopied] = useState(false);
  const onClick = async () => {
    const r = await fetch(path + ".md");
    const text = await r.text();
    await navigator.clipboard.writeText(text);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  };
  return (
    <button
      type="button"
      onClick={onClick}
      className="rounded-[3px] border border-rule bg-bg-card px-2.5 py-1 font-mono text-[11px] text-text-secondary hover:text-text-primary"
    >
      {copied ? "Copied" : "Copy as Markdown"}
    </button>
  );
}
```

- [ ] **Step 3: Wire into DocsPageHeader**

```tsx
import { CopyPageButton } from "./CopyPageButton";
// inside header, after H1:
<div className="mt-3 flex items-center justify-between">
  <p className="...">{description}</p>
  <CopyPageButton path={path} />
</div>;
```

Pass `path` from page.tsx (`'/docs' + (slug.length ? '/' + slug.join('/') : '')`).

- [ ] **Step 4: Add `<link rel="alternate">` via metadata**

In `[[...slug]]/page.tsx`:

```tsx
import type { Metadata } from "next";
export async function generateMetadata({
  params,
}: {
  params: Promise<Params>;
}): Promise<Metadata> {
  const { slug = [] } = await params;
  const path = "/docs" + (slug.length ? "/" + slug.join("/") : "");
  return {
    alternates: { types: { "text/markdown": `${path}.md` } },
    other: { "x-llms-txt": "/llms.txt" },
  };
}
```

- [ ] **Step 5: Run test, PASS, commit**

```bash
git commit -m "feat(website): Copy as Markdown button + <link rel=alternate>"
```

### Task 25: `/llms.txt` route

**Files:**

- Create: `apps/website/app/llms.txt/route.ts`
- Test: `apps/website/app/llms.txt/__tests__/route.test.ts`

- [ ] **Step 1: Failing test (against fixture root)**

```ts
import { describe, it, expect } from "vitest";
import path from "node:path";
import { buildLlmsTxt } from "../build";

const ROOT = path.join(
  __dirname,
  "../../../lib/docs/__tests__/__fixtures__/content/docs",
);

describe("buildLlmsTxt", () => {
  it("groups by frontmatter.nav and lists pages", async () => {
    const txt = await buildLlmsTxt(ROOT, [
      {
        title: "Alpha",
        items: [
          { title: "Alpha overview", href: "/docs/alpha" },
          { title: "One", href: "/docs/alpha/one" },
        ],
      },
    ]);
    expect(txt).toMatch(/^# Pretable Docs\n\n> /m);
    expect(txt).toMatch(/## Alpha/);
    expect(txt).toMatch(
      /- \[Alpha overview\]\(\/docs\/alpha\.md\): Alpha overview/,
    );
  });
});
```

- [ ] **Step 2: Implement helper + route**

```ts
// apps/website/app/llms.txt/build.ts
import { enumerateDocs } from "../../lib/docs/enumerate";
import type { DocsNavSection } from "../docs/_nav";

export async function buildLlmsTxt(
  root: string,
  nav: DocsNavSection[],
): Promise<string> {
  const pages = await enumerateDocs(root);
  const bySlug = new Map(
    pages.map((p) => [
      "/docs" + (p.slug.length ? "/" + p.slug.join("/") : ""),
      p,
    ]),
  );
  const lines: string[] = [
    "# Pretable Docs",
    "",
    "> The drop-in React data grid built for streaming.",
    "",
  ];
  for (const group of nav) {
    lines.push(`## ${group.title}`);
    for (const item of group.items) {
      const page = bySlug.get(item.href);
      if (!page) continue;
      lines.push(
        `- [${page.frontmatter.title}](${item.href}.md): ${page.frontmatter.description}`,
      );
    }
    lines.push("");
  }
  return lines.join("\n");
}
```

```ts
// apps/website/app/llms.txt/route.ts
import path from "node:path";
import { buildLlmsTxt } from "./build";
import { docsNav } from "../docs/_nav";

const ROOT = path.join(process.cwd(), "content/docs");

export const dynamic = "force-static";
export const revalidate = 3600;

export async function GET() {
  const text = await buildLlmsTxt(ROOT, docsNav);
  return new Response(text, {
    headers: {
      "Content-Type": "text/plain; charset=utf-8",
      "Cache-Control": "public, s-maxage=3600",
    },
  });
}
```

- [ ] **Step 3: Run test, PASS**

- [ ] **Step 4: Browser smoke**

Visit `/llms.txt`. Expect plain text starting with `# Pretable Docs`.

- [ ] **Step 5: Commit**

```bash
git commit -m "feat(website): /llms.txt auto-generated index"
```

### Task 26: `/llms-full.txt`

**Files:**

- Create: `apps/website/app/llms-full.txt/{route.ts,build.ts}`
- Test: `apps/website/app/llms-full.txt/__tests__/route.test.ts`

- [ ] **Step 1: Failing test** — for fixture root, output includes every page's `# title` and body separated by `---`.

- [ ] **Step 2: Implement**

```ts
// apps/website/app/llms-full.txt/build.ts
import fs from "node:fs/promises";
import { enumerateDocs } from "../../lib/docs/enumerate";

const FRONTMATTER_RE = /^---\n[\s\S]*?\n---\n?/;

export async function buildLlmsFullTxt(root: string): Promise<string> {
  const pages = await enumerateDocs(root);
  const sections: string[] = [];
  for (const p of pages) {
    const raw = await fs.readFile(p.filePath, "utf8");
    const body = raw.replace(FRONTMATTER_RE, "");
    sections.push(
      `# ${p.frontmatter.title}\n\n${p.frontmatter.description}\n\n${body}`,
    );
  }
  return sections.join("\n\n---\n\n");
}
```

```ts
// apps/website/app/llms-full.txt/route.ts
import path from "node:path";
import { buildLlmsFullTxt } from "./build";

const ROOT = path.join(process.cwd(), "content/docs");
export const dynamic = "force-static";
export const revalidate = 3600;

export async function GET() {
  const text = await buildLlmsFullTxt(ROOT);
  return new Response(text, {
    headers: {
      "Content-Type": "text/plain; charset=utf-8",
      "Cache-Control": "public, s-maxage=3600",
    },
  });
}
```

- [ ] **Step 3: Run test PASS, smoke browser, commit**

```bash
git commit -m "feat(website): /llms-full.txt"
```

### Task 27: HTTP `Link: rel="llms-txt"` header

**Files:**

- Modify: `apps/website/next.config.ts`

- [ ] **Step 1: Add headers**

```ts
async headers() {
  return [
    {
      source: "/docs/:path*",
      headers: [{ key: "Link", value: '</llms.txt>; rel="llms-txt"' }],
    },
  ];
}
```

- [ ] **Step 2: Smoke**

```bash
curl -sI http://localhost:3000/docs | grep -i ^link
```

Expected: `link: </llms.txt>; rel="llms-txt"`.

- [ ] **Step 3: Commit**

```bash
git commit -m "feat(website): add Link rel=llms-txt header on /docs/*"
```

---

## Phase 10: Search

### Task 28: Search index route

**Files:**

- Create: `apps/website/app/docs/search-index.json/route.ts`
- Create: `apps/website/lib/docs/search-index.ts`
- Test: `apps/website/lib/docs/__tests__/search-index.test.ts`

- [ ] **Step 1: Failing test (fixture)**

```ts
import { describe, it, expect } from "vitest";
import path from "node:path";
import { buildSearchIndex } from "../search-index";

const ROOT = path.join(__dirname, "__fixtures__/content/docs");

describe("buildSearchIndex", () => {
  it("returns one entry per page with title, headings, body excerpt", async () => {
    const idx = await buildSearchIndex(ROOT);
    expect(idx).toHaveLength(2);
    expect(idx[0]).toMatchObject({ title: "Alpha", nav: "Alpha" });
    expect(typeof idx[0].body).toBe("string");
  });
});
```

- [ ] **Step 2: Implement**

````ts
// apps/website/lib/docs/search-index.ts
import fs from "node:fs/promises";
import { enumerateDocs } from "./enumerate";
import { extractHeadings } from "./extract-headings";

const FRONTMATTER_RE = /^---\n[\s\S]*?\n---\n?/;
const FENCE_RE = /```[\s\S]*?```/g;
const TAG_RE = /<[^>]+>/g;

export interface SearchEntry {
  slug: string;
  title: string;
  description: string;
  nav: string;
  headings: string[];
  body: string;
}

export async function buildSearchIndex(root: string): Promise<SearchEntry[]> {
  const pages = await enumerateDocs(root);
  const out: SearchEntry[] = [];
  for (const p of pages) {
    const raw = await fs.readFile(p.filePath, "utf8");
    const body = raw
      .replace(FRONTMATTER_RE, "")
      .replace(FENCE_RE, "")
      .replace(TAG_RE, "")
      .replace(/\s+/g, " ")
      .trim()
      .slice(0, 2000);
    out.push({
      slug: "/docs" + (p.slug.length ? "/" + p.slug.join("/") : ""),
      title: p.frontmatter.title,
      description: p.frontmatter.description,
      nav: p.frontmatter.nav,
      headings: extractHeadings(raw).map((h) => h.text),
      body,
    });
  }
  return out;
}
````

```ts
// apps/website/app/docs/search-index.json/route.ts
import path from "node:path";
import { buildSearchIndex } from "../../../lib/docs/search-index";

const ROOT = path.join(process.cwd(), "content/docs");
export const dynamic = "force-static";

export async function GET() {
  const idx = await buildSearchIndex(ROOT);
  return Response.json(idx);
}
```

- [ ] **Step 3: Test PASS, commit**

```bash
git commit -m "feat(website): docs search index"
```

### Task 29: `DocsSearch` Cmd+K palette

**Files:**

- Create: `apps/website/app/components/docs/DocsSearch.tsx`
- Modify: `apps/website/app/components/docs/DocsShell.tsx` (mount once at root)
- Test: `apps/website/app/components/docs/__tests__/DocsSearch.test.tsx`

- [ ] **Step 1: Failing test**

```tsx
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { DocsSearch } from "../DocsSearch";

describe("DocsSearch", () => {
  it("opens on Cmd+K, closes on Esc", async () => {
    global.fetch = vi.fn().mockResolvedValue({
      json: () =>
        Promise.resolve([
          {
            slug: "/docs/g",
            title: "Grid",
            description: "d",
            nav: "Grid",
            headings: [],
            body: "",
          },
        ]),
    });
    render(<DocsSearch />);
    fireEvent.keyDown(window, { key: "k", metaKey: true });
    await waitFor(() => expect(screen.getByRole("dialog")).toBeInTheDocument());
    fireEvent.keyDown(window, { key: "Escape" });
    expect(screen.queryByRole("dialog")).not.toBeInTheDocument();
  });
  it("filters results by title match", async () => {
    global.fetch = vi.fn().mockResolvedValue({
      json: () =>
        Promise.resolve([
          {
            slug: "/docs/streaming",
            title: "Streaming",
            description: "d",
            nav: "Streaming",
            headings: [],
            body: "",
          },
          {
            slug: "/docs/theming",
            title: "Theming",
            description: "d",
            nav: "Theming",
            headings: [],
            body: "",
          },
        ]),
    });
    render(<DocsSearch />);
    fireEvent.keyDown(window, { key: "k", metaKey: true });
    const input = await screen.findByRole("combobox");
    fireEvent.change(input, { target: { value: "stream" } });
    expect(
      await screen.findByRole("link", { name: /Streaming/ }),
    ).toBeInTheDocument();
    expect(
      screen.queryByRole("link", { name: /Theming/ }),
    ).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Implement**

```tsx
"use client";
import fuzzysort from "fuzzysort";
import Link from "next/link";
import { useEffect, useRef, useState } from "react";

interface SearchEntry {
  slug: string;
  title: string;
  description: string;
  nav: string;
  headings: string[];
  body: string;
}

export function DocsSearch() {
  const [open, setOpen] = useState(false);
  const [q, setQ] = useState("");
  const [index, setIndex] = useState<SearchEntry[] | null>(null);
  const inputRef = useRef<HTMLInputElement>(null);

  useEffect(() => {
    const onKey = (e: KeyboardEvent) => {
      if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "k") {
        e.preventDefault();
        setOpen(true);
      } else if (e.key === "Escape") {
        setOpen(false);
      }
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, []);

  useEffect(() => {
    if (!open || index) return;
    fetch("/docs/search-index.json")
      .then((r) => r.json())
      .then(setIndex);
  }, [open, index]);

  useEffect(() => {
    if (open) inputRef.current?.focus();
  }, [open]);

  if (!open) return null;

  const results = index
    ? q
      ? fuzzysort
          .go(q, index, {
            keys: ["title", "headings", "description", "body"],
            scoreFn: (a) =>
              Math.max(
                (a[0]?.score ?? -1000) * 4,
                (a[1]?.score ?? -1000) * 2,
                a[2]?.score ?? -1000,
                (a[3]?.score ?? -1000) * 0.5,
              ),
          })
          .map((r) => r.obj)
          .slice(0, 8)
      : index.slice(0, 8)
    : [];

  return (
    <div
      role="dialog"
      aria-modal="true"
      className="fixed inset-0 z-50 backdrop-blur-sm"
    >
      <div
        className="absolute inset-0 bg-black/40"
        onClick={() => setOpen(false)}
      />
      <div className="relative mx-auto mt-[15vh] max-w-[640px] rounded-md border border-rule bg-bg-card shadow-lg">
        <input
          ref={inputRef}
          role="combobox"
          aria-expanded
          aria-controls="search-results"
          value={q}
          onChange={(e) => setQ(e.target.value)}
          placeholder="Search the docs"
          className="w-full border-b border-rule bg-transparent px-4 py-3 font-mono text-[14px] outline-none"
        />
        <ul id="search-results" className="max-h-[60vh] overflow-y-auto py-2">
          {results.map((r) => (
            <li key={r.slug}>
              <Link
                href={r.slug}
                onClick={() => setOpen(false)}
                className="block px-4 py-2 hover:bg-bg-raised"
              >
                <div className="font-display text-[14px] text-text-primary">
                  {r.title}
                </div>
                <div className="font-mono text-[11px] text-text-dim">
                  {r.nav}
                </div>
              </Link>
            </li>
          ))}
        </ul>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Mount in DocsShell** — render `<DocsSearch />` once at the top.

- [ ] **Step 4: Run test, PASS, browser smoke** — Cmd+K opens, type "stream", click result.

- [ ] **Step 5: Commit**

```bash
git commit -m "feat(website): docs Cmd+K search palette"
```

---

## Phase 11: Prev/Next

### Task 30: `DocsPrevNext` component

**Files:**

- Create: `apps/website/app/components/docs/DocsPrevNext.tsx`
- Modify: `apps/website/app/docs/[[...slug]]/page.tsx`
- Test: `apps/website/app/components/docs/__tests__/DocsPrevNext.test.tsx`

- [ ] **Step 1: Failing test** — render with prev only, next only, both — verify links / no links present.

- [ ] **Step 2: Implement**

```tsx
import Link from "next/link";
import type { PrevNext } from "../../../lib/docs/prev-next";

export function DocsPrevNext({ prev, next }: PrevNext) {
  if (!prev && !next) return null;
  return (
    <nav
      aria-label="Page navigation"
      className="mt-12 flex items-center justify-between gap-4 border-t border-rule pt-6"
    >
      {prev ? (
        <Link
          href={prev.href}
          className="group flex flex-col text-text-secondary hover:text-text-primary"
        >
          <span className="font-mono text-[11px] uppercase tracking-[0.14em] text-text-dim">
            ← Prev
          </span>
          <span className="font-display text-[14px]">{prev.title}</span>
        </Link>
      ) : (
        <span />
      )}
      {next ? (
        <Link
          href={next.href}
          className="group flex flex-col items-end text-text-secondary hover:text-text-primary"
        >
          <span className="font-mono text-[11px] uppercase tracking-[0.14em] text-text-dim">
            Next →
          </span>
          <span className="font-display text-[14px]">{next.title}</span>
        </Link>
      ) : (
        <span />
      )}
    </nav>
  );
}
```

- [ ] **Step 3: Wire into page.tsx**

```tsx
import { resolvePrevNext } from "../../../lib/docs/prev-next";
import { docsNav } from "../_nav";
import { DocsPrevNext } from "../../components/docs/DocsPrevNext";
// inside HTML branch, after {result.content}:
const path = "/docs" + (slug.length ? "/" + slug.join("/") : "");
const { prev, next } = resolvePrevNext(path, docsNav);
// <DocsPrevNext prev={prev} next={next} />
```

- [ ] **Step 4: Test PASS, commit**

```bash
git commit -m "feat(website): docs prev/next navigation"
```

---

## Phase 12: Content migration

### Task 31: Move all 24 pages to `content/docs/`

**Files:**

- Create: 24 files under `apps/website/content/docs/**`
- Delete: corresponding `apps/website/app/docs/<group>/[**]/page.mdx` files (after move)

**Migration map:**

| From                                           | To                                                                            |
| ---------------------------------------------- | ----------------------------------------------------------------------------- |
| `app/docs/page.mdx`                            | `content/docs/getting-started/index.mdx` (already exists from Task 8 smoke)   |
| `app/docs/getting-started/page.mdx`            | `content/docs/getting-started/index.mdx` (overwrite if smoke version is stub) |
| `app/docs/getting-started/concepts/page.mdx`   | `content/docs/getting-started/concepts.mdx`                                   |
| `app/docs/grid/page.mdx`                       | `content/docs/grid/index.mdx`                                                 |
| `app/docs/grid/pretable-component/page.mdx`    | `content/docs/grid/pretable-component.mdx`                                    |
| `app/docs/grid/pretable-surface/page.mdx`      | `content/docs/grid/pretable-surface.mdx`                                      |
| `app/docs/grid/custom-rendering/page.mdx`      | `content/docs/grid/custom-rendering.mdx`                                      |
| `app/docs/grid/density-helpers/page.mdx`       | `content/docs/grid/density-helpers.mdx`                                       |
| `app/docs/grid/api-reference/page.mdx`         | `content/docs/grid/api-reference.mdx`                                         |
| `app/docs/streaming/page.mdx`                  | `content/docs/streaming/index.mdx`                                            |
| `app/docs/streaming/element-streams/page.mdx`  | `content/docs/streaming/element-streams.mdx`                                  |
| `app/docs/streaming/partial-streams/page.mdx`  | `content/docs/streaming/partial-streams.mdx`                                  |
| `app/docs/streaming/parsers/page.mdx`          | `content/docs/streaming/parsers.mdx`                                          |
| `app/docs/streaming/api-reference/page.mdx`    | `content/docs/streaming/api-reference.mdx`                                    |
| `app/docs/theming/page.mdx`                    | `content/docs/theming/index.mdx`                                              |
| `app/docs/theming/pick-a-theme/page.mdx`       | `content/docs/theming/pick-a-theme.mdx`                                       |
| `app/docs/theming/override-tokens/page.mdx`    | `content/docs/theming/override-tokens.mdx`                                    |
| `app/docs/theming/light-dark/page.mdx`         | `content/docs/theming/light-dark.mdx`                                         |
| `app/docs/theming/density/page.mdx`            | `content/docs/theming/density.mdx`                                            |
| `app/docs/theming/custom-themes/page.mdx`      | `content/docs/theming/custom-themes.mdx`                                      |
| `app/docs/theming/tailwind-css-in-js/page.mdx` | `content/docs/theming/tailwind-css-in-js.mdx`                                 |
| `app/docs/theming/token-reference/page.mdx`    | `content/docs/theming/token-reference.mdx`                                    |

- [ ] **Step 1: For each file** — copy content, prepend frontmatter:

```yaml
---
title: <existing H1 text>
description: <one-sentence summary derived from first paragraph>
nav: <Group from _nav.ts that contains this href>
order: <position within group>
---
```

- [ ] **Step 2: Delete the old `app/docs/<group>/[**]/page.mdx` files\*\*

```bash
git rm $(find apps/website/app/docs -mindepth 2 -name 'page.mdx')
```

- [ ] **Step 3: Verify all routes still resolve**

```bash
pnpm --filter @pretable/app-website dev
```

For each href in `_nav.ts`, visit it. Expect H1 + description + content rendered through new pipeline.

- [ ] **Step 4: Run unit + integration tests**

```bash
pnpm --filter @pretable/app-website test
```

Expected: all green.

- [ ] **Step 5: Commit**

```bash
git add apps/website/content/ apps/website/app/docs/
git commit -m "feat(website): migrate 24 docs pages to content/docs tree"
```

---

## Phase 13: Marketing migration

### Task 32: Migrate `CodeExample` to `<Example>`

**Files:**

- Modify: `apps/website/app/components/CodeExample.tsx`
- Delete: `apps/website/app/components/CodeTabs.tsx` (if no other consumer)

- [ ] **Step 1: Find other consumers of `CodeTabs`**

Run: `grep -r "CodeTabs" apps/website/app | grep -v CodeExample`
Expected: no other consumers, OR list of files that need migration first.

- [ ] **Step 2: Replace CodeExample body**

```tsx
// apps/website/app/components/CodeExample.tsx
import { streamingChatGrid } from "../../content/examples/streaming-chat-grid";
import { Example } from "./docs/mdx/Example";

export function CodeExample() {
  return (
    <section className="text-text-primary px-7 py-16 md:px-10 md:py-28">
      <div className="mx-auto max-w-[1240px]">
        <p className="font-mono text-[11px] uppercase tracking-[0.14em] text-accent">
          06 · for engineers
        </p>
        <h2 className="mt-4 font-display text-[36px] leading-[1.05] tracking-[-0.025em] md:text-[44px]">
          For engineers: how it looks in your codebase.
        </h2>
        <p className="mt-5 max-w-[56ch] font-display text-[17px] leading-[1.55] text-text-secondary">
          Connect any token-streaming source — OpenAI Responses, Anthropic, or
          your own SSE — to a pretable grid. Selection survives every chunk.
        </p>
        <div className="mt-8">
          <Example example={streamingChatGrid} defaultOpen showLive />
        </div>
        <p className="mt-5 font-mono text-[12px] text-text-muted">
          Full reference:{" "}
          <a
            href="/docs/streaming"
            className="text-accent-deep underline-offset-2 hover:underline"
          >
            /docs/streaming
          </a>
        </p>
      </div>
    </section>
  );
}
```

- [ ] **Step 3: Delete `CodeTabs.tsx` and `CodeBlock.tsx`** (if `CodeBlock` only feeds the marketing example, replace with the new docs CodeBlock; otherwise keep until consumers migrate)

```bash
git rm apps/website/app/components/CodeTabs.tsx
```

- [ ] **Step 4: Run all tests + browser smoke**

```bash
pnpm --filter @pretable/app-website test
pnpm --filter @pretable/app-website dev
```

Visit `/`. Verify the chat-grid section renders the live `<ChatGrid>` plus the source disclosure.

- [ ] **Step 5: Update existing test** — `apps/website/app/components/__tests__/CodeExample.test.tsx` (if exists) — replace assertions to check live demo + Example component presence.

- [ ] **Step 6: Commit**

```bash
git add apps/website/app/components/CodeExample.tsx apps/website/app/components/__tests__/
git commit -m "refactor(website): marketing CodeExample uses <Example> with live demo"
```

---

## Phase 14: E2E + verification

### Task 33: Playwright smoke

**Files:**

- Create: `apps/website/e2e/docs.spec.ts`

- [ ] **Step 1: Write tests**

```ts
// apps/website/e2e/docs.spec.ts
import { test, expect } from "@playwright/test";

test("docs page renders sidebar, content, TOC", async ({ page }) => {
  await page.goto("/docs/grid/pretable-component");
  await expect(page.getByRole("heading", { level: 1 })).toContainText(
    "Pretable",
  );
  await expect(
    page.getByRole("link", { name: "<Pretable> component" }).first(),
  ).toHaveAttribute("aria-current", "page");
  await expect(page.getByText("On this page")).toBeVisible();
});

test("Copy as Markdown button writes clipboard", async ({ page, context }) => {
  await context.grantPermissions(["clipboard-read", "clipboard-write"]);
  await page.goto("/docs/grid/pretable-component");
  await page.getByRole("button", { name: /copy as markdown/i }).click();
  const text = await page.evaluate(() => navigator.clipboard.readText());
  expect(text).toMatch(/^# /);
});

test("Cmd+K opens search and navigates", async ({ page }) => {
  await page.goto("/docs");
  await page.keyboard.press("Meta+k");
  await expect(page.getByRole("dialog")).toBeVisible();
  await page.getByRole("combobox").fill("streaming");
  await page
    .getByRole("link", { name: /Streaming/i })
    .first()
    .click();
  await expect(page).toHaveURL(/\/docs\/streaming/);
});

test("/docs/<slug>.md returns markdown", async ({ request }) => {
  const r = await request.get("/docs/grid/pretable-component.md");
  expect(r.headers()["content-type"]).toMatch(/text\/markdown/);
  expect(await r.text()).toMatch(/^# /);
});

test("/llms.txt returns index", async ({ request }) => {
  const r = await request.get("/llms.txt");
  expect(r.status()).toBe(200);
  expect(await r.text()).toMatch(/^# Pretable Docs/);
});

test("mobile: drawer toggles", async ({ page }) => {
  await page.setViewportSize({ width: 375, height: 800 });
  await page.goto("/docs");
  await page.getByRole("button", { name: /menu/i }).click();
  await expect(page.getByRole("dialog")).toBeVisible();
});
```

- [ ] **Step 2: Run**

```bash
pnpm --filter @pretable/app-website e2e -- docs.spec.ts
```

Expected: all green. Fix any failures inline before committing.

- [ ] **Step 3: Commit**

```bash
git add apps/website/e2e/docs.spec.ts
git commit -m "test(website): e2e docs site"
```

### Task 34: Final verification

- [ ] **Step 1: All checks**

```bash
pnpm typecheck
pnpm lint
pnpm test
pnpm format
pnpm build
```

Each must succeed.

- [ ] **Step 2: Manual smoke checklist**

- [ ] `/docs` redirects/renders Getting Started
- [ ] `/docs/grid` renders Grid index
- [ ] `/docs/grid/pretable-component` shows sidebar active state, TOC headings, prev/next
- [ ] Click `[Copy as Markdown]` → clipboard has markdown
- [ ] Cmd+K → type → enter navigates
- [ ] Mobile sidebar drawer works
- [ ] `/docs/grid/pretable-component.md` returns text
- [ ] `/llms.txt` and `/llms-full.txt` return text
- [ ] `curl -I /docs/grid` shows `Link: </llms.txt>; rel="llms-txt"`
- [ ] Marketing `/` chat-grid section shows live demo
- [ ] No console errors in any route

- [ ] **Step 3: Open PR**

```bash
git push -u origin assistant/nice-cerf-cac464
gh pr create --title "feat(website): docs site v1 — 3-col layout, MDX components, AI-forward" --body "$(cat <<'EOF'
## Summary
- New 3-col docs layout with scrollable sidebar and TOC scrollspy
- Migrated 24 docs pages from per-folder page.mdx to content/docs tree
- MDX component library: Callout, Steps, Tabs, CodeGroup, Card, Frame, Prompt, Example
- <Example> with live demo + source disclosure + copy-all
- AI-forward: Copy as Markdown, /docs/<slug>.md, /llms.txt, /llms-full.txt
- Cmd+K client-side search with fuzzy ranking
- Marketing CodeExample now uses <Example> for a real live demo

## Test plan
- [ ] `pnpm test` — unit + integration
- [ ] `pnpm e2e` — Playwright smoke
- [ ] Manual: visit each href in _nav.ts, confirm renders

🤖 Generated with [Assistant Code](https://assistant.com/assistant-code)
EOF
)"
```

---

## Self-Review

**Spec coverage check:**

- ✅ 3-col layout with dimensions — Tasks 10, 13
- ✅ Sidebar (sticky, scrollable, active state) — Task 12
- ✅ TOC scrollspy — Task 14
- ✅ Mobile drawer — Task 13
- ✅ MDX pipeline (compileMDX + plugins) — Tasks 6, 7
- ✅ Component registry: Callout, Steps, Tabs, CodeGroup, Card/CardGroup, Frame, Prompt — Tasks 15–21
- ✅ Example (live + source + copy-all) — Tasks 22, 23
- ✅ Per-page .md via middleware + format=md branch — Task 9
- ✅ /llms.txt — Task 25
- ✅ /llms-full.txt — Task 26
- ✅ Discovery `<link rel=alternate>` + HTTP `Link` header — Tasks 24, 27
- ✅ Cmd+K search — Tasks 28, 29
- ✅ Content migration — Task 31
- ✅ Marketing CodeExample migration — Task 32
- ✅ Frontmatter contract (`title`, `description`, `nav`, `order`) — Task 1, used throughout
- ✅ Visual identity (Alpenglow) — applied throughout
- ✅ E2E + final verification — Tasks 33, 34

**Spec gaps found:** none.

**Type consistency:** `DocsFrontmatter`, `ExampleDef`, `DocsHeading`, `DocsNavSection`, `DocsNavItem`, `LoadResult`, `PrevNext`, `SearchEntry` are defined once and reused. `?raw` import for example sources is consistently used.

**Placeholder scan:** plan does not contain "TBD" (verified by grep). One forward-pointer in Task 7 ("file titles parsed in a later pass via rehype meta — leave undefined here") — acceptable; the field is optional and the immediate behavior (no filename) is well-defined.
